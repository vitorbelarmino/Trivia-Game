const INIT_STATE = {
  player: {
    name: '',
    assertions: 0,
    score: 0,
    gravatarEmail: '',
  },
};

const player = (state = INIT_STATE, action) => {
  switch (action.type) {
  default: {
    return state;
  }
  }
};

export default player;
